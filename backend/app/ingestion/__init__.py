"""Ingestion package.

Purpose
-------
Turns raw source files into clean, chunked, searchable records: file loaders,
the source-aware chunker, document classification, and the pipeline that ties
them together.
"""
