"""Service layer.

Purpose
-------
Reusable services that sit between the API and the core engine: admin operations,
chat storage, configuration overrides, query logging, source aggregation, and
status.
"""
