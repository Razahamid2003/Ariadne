"""Vision package.

Purpose
-------
Holds the optional local vision client. Vision is off by default; when disabled,
images and image-only pages are catalogued rather than captioned, so a vision model
is never required.
"""
